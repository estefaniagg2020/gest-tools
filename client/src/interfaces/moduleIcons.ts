export type ModuleIconCategory = "calendarios" | "personas" | "servicios" | "inventario" | "clientes";

export interface ModuleIconsConfig {
  calendarios: string;
  personas: string;
  servicios: string;
  inventario: string;
  clientes: string;
}
